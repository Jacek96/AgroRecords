import 'package:flutter/material.dart';

class AddRecordPageContent extends StatelessWidget {
  const AddRecordPageContent({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dwa'),
    );
  }
}
